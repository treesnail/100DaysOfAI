# day077 课后习题：M7 前四课复盘（Math-D1 ~ M7-D2）

> 做之前请先读 [教程](../教程/教程.md)。
> 全部题目都可以在**离线**环境里完成：纯 Python 算术，零网络、零依赖。
>
> 建议做法：**先合上教程自己回答**（复习日的核心是提取练习），再对照
> [参考答案](../习题答案/习题答案.md) 与对应那一天的原文。
> 标了「推导」的题目请真的在纸上写一遍——它们是这四天最值钱的四个推导。

---

## 一、选择题（6 题）

**1.** 关于缩放的用途，正确的说法是：

- A. 除以 `√d_k` 是为了防止点积溢出
- B. 除以 `√d_k` 是为了把打分的方差拉回与维度无关
- C. 除以 `√d_k` 可以让 softmax 的输出更均匀
- D. 除以 `√d_k` 是数值稳定性的要求，与训练无关

**2.** 关于掩码，正确的说法是：

- A. 把被掩码位置的打分写成 `-inf`，再一次性 softmax 是最简洁的做法
- B. 被掩码位置必须**恰好是 0.0**，因为它让"那些位置的梯度是 0"成为一条推论
- C. 只要前向输出正确，掩码怎么写都可以
- D. `causal=True` 与显式掩码可以同时给，谁生效取决于实现顺序

**3.** 关于多头与参数量，正确的说法是：

- A. `heads` 越大参数量越大
- B. `heads` 不改变参数量，因为四个投影矩阵的形状里没有 `heads`
- C. `heads` 只影响 `W_o` 的形状
- D. 多头比单头多出 `heads` 倍的参数量，这正是它更强的原因

**4.** `d_k = 12`、`heads = 3` 时，多头真正使用的缩放系数是：

- A. `1/√12`
- B. `1/√4`
- C. `1/√3`
- D. `1/12`

**5.** 关于梯度校验的容差，正确的说法是：

- A. 容差越小越严格，因此总是取 `1e-12`
- B. 容差必须高于数值差分的分辨率，否则校验必然失败
- C. 容差只取决于机器精度
- D. 容差可以随便设，只要"真的写错了"能红就行

**6.** 关于 `heads=1` 时的头间差异读数，正确的说法是：

- A. `mean_total_variation = 0` 且 `degenerate = True`
- B. `mean_total_variation = 0` 且 `degenerate = False`
- C. `mean_total_variation = 1` 且 `degenerate = True`
- D. 这个读数在 `heads=1` 时没有定义

---

## 二、简答题（5 题）

**7.** 用一句话说明"为什么要除 `√d_k`"，并写出它的方差论证要点
（提示：`q·k` 在 `q`、`k` 各维独立同分布时的方差是多少）。

**8.** 四天一共出现过**四处"漏掉不会报错"**的地方（day075 三处 + day076 一处）。
请把它们列出来，并各写一句"症状"。

**9.** 四天里有五处常量被写了两遍（`scale`、`GRADIENT_TOLERANCE`、
`DEFAULT_INIT_SCALE`、`PARAMETER_BLOCKS`、`GRADIENT_SOURCES`）。
为什么"两处分别跑一遍看起来都对"**不是**证据？正确的做法是什么？

**10.** 数值差分与解析梯度各自的"两个优点 + 一个死穴"是什么？
为什么它们必须**同时存在**？

**11.** 复习日的教程为什么坚持"先合上资料自己复述，再打开原文对照"？
请写出这条纪律的认知科学依据（一个术语即可）以及它在工程上的等价物。

---

## 三、诊断题（2 题）

**12.**（诊断）下面这段多头反向的片段会给出"形状正确、损失也在降"的结果，
但它在 `heads > 1` 时是错的。请指出错误、说明它在 `heads = 1` 时为什么**完全看不出来**，
以及哪两项梯度校验会红：

```python
        # ③ 打分：raw_h = Q_h·K_hᵀ
        grad_queries = matmul(grad_raw, forward.head_keys[0])            # ← 这两行
        grad_keys = matmul(transpose(grad_raw), forward.head_queries[0])
```

**13.**（诊断）一份训练报告里出现下面两个读数，请分别给出诊断与下一步动作：

```text
report A   损失 0.1634 → 0.0000   命中率 38% → 100%   峰值 0.232 → 0.444   头间差异 0.0000（heads=2）
report B   损失 0.1634 → 0.1630   命中率 38% →  12%   峰值 0.232 → 0.587   头间差异 0.0421
```

---

## 四、推导题（2 题）

**14.**（推导）softmax 的反向：设 `p = softmax(z)`，上游梯度是 `g`。
请从完整雅可比 `∂p_i/∂z_j = p_i(δ_ij − p_j)` 出发，推出

```text
dScores_i = p_i (g_i − Σ_j g_j p_j)
```

并用 `p = (0.5, 0.5)`、`g = (1, 0)` 验证你的结果（给出两个分量）。
再回答：为什么**不能**简写成 `dScores = g`？

**15.**（推导）多头的可达集合：

- 把单头第 `i` 行的输出写成 `Σ_j w_j · contrib_j`，写出 `contrib_j` 的定义；
- 说明为什么单头的可达集合是 `conv{ contrib_j }`；
- 写出多头可达集合的两种等价形式（闵可夫斯基和 / 网格点集的凸包）；
- 用教程第九章的样本手算：单头凸包是哪条线段？目标 `(1,1)` 到它的距离是多少？
  多头凸包是什么？并写出兑现目标的两份分布。
